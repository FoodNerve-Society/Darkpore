import React from 'react';

export default function ShowcaseCarousel({ cards, accentColor, onComplete, onActiveIndexChange, title, description, titleIcon }: any) {
    return <div>Showcase Carousel</div>;
}
